def sum (a,b)
{
  echo" The sum of ${a} and ${b} is ${a+b}"
  echo" The multiplication of ${a+b} and ${b} is ${a+b*b}"
}
def mul (c,d)
{
   echo" The mul of ${c} and ${d} is ${c*d}"
  echo" The multiplication of ${c*d} and ${d} is ${c*d*d}"
}
